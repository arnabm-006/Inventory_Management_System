# Inventory_Management_System
This Python script is a console-based Inventory Management System that allows users to track products using foundational CRUD (Create, Read, Update, Delete) operations. It stores data dynamically in memory using a nested dictionary structure, where each unique Item ID serves as a key pointing to sub-details like product name, price, and quantity.
